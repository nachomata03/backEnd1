<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{product.title}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-800">

    <header class="bg-white shadow p-4">
        <div class="max-w-5xl mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold">Detalle del Producto</h1>
            <a href="/products" class="text-blue-600 hover:underline">← Volver</a>
        </div>
    </header>

    <main class="max-w-5xl mx-auto mt-8 bg-white rounded-lg shadow p-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">

            <!-- Imagen del producto -->
            <div>
                <img src="{{producto.thumbnail}}" alt="{{producto.title}}"
                    class="rounded-lg w-full h-auto object-cover" />
            </div>

            <!-- Información del producto -->
            <div>
                <h2 class="text-3xl font-bold mb-2">{{producto.title}}</h2>
                <p class="text-gray-700 mb-4">{{producto.description}}</p>
                <p class="text-lg font-semibold text-green-600 mb-2">Precio: ${{producto.price}}</p>
                <p class="text-sm text-gray-500 mb-2">Código: {{producto.code}}</p>
                <p class="text-sm text-gray-500 mb-4">Categoría: {{producto.category}}</p>

                <button class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 transition">
                    Agregar al carrito
                </button>
            </div>

        </div>
    </main>

</body>

</html>