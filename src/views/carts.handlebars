<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
</head>
<body>
    <h1>Carrito</h1>

    {{#if cart.products.length}}
        <ul>
            {{#each cart.products}}
                <li>{{this.id.title}} - Cantidad: {{this.quantity}}</li>
            {{/each}}
        </ul>
    {{else}}
        <p>El carrito está vacío</p>
    {{/if}}

</body>
</html>